# Pin the exact toolchain so local dev and CI judge the code with the
# same eyes — an unpinned CI clippy will eventually flag lints the
# local one cannot see. Bumping this file is a deliberate, reviewable
# commit.
[toolchain]
channel = "1.97.0"
components = ["rustfmt", "clippy"]
